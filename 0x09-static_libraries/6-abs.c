#include "main.h"

/**
 * _abs - calculates the absolute value of an integer
 * @n: the int to be checked
 *
 * Return: the absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

