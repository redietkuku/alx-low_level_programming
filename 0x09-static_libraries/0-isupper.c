#include "main.h"

/**
 * _isupper - uppercase letters
 * @c: character
 *
 * Return: 0 if success 1 if fail
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'B')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
